# 🐍 My Python Coding Buddy

A lightweight AI-powered CLI tool that helps you generate clean Python code snippets using **Groq Cloud** and **Llama 3.1**.

## 🚀 Why I built this?
While working on my Kubernetes projects, I wanted a fast way to generate boilerplate Python scripts without leaving my terminal. This tool provides instant, optimized code examples.

## 🛠️ Tech Stack
- **Language:** Python
- **LLM:** Llama 3.1 (via Groq LPU for ultra-fast inference)
- **Security:** Managed via Environment Variables (`python-dotenv`)

## ⚙️ How to Setup
1. 1. Clone the repo: git clone 'https://github.com/ygtozbek/Agentic-Python-Assistant.git'
2. Install requirements: `pip install -r requirements.txt`
3. Create a `.env` file and add your `GROQ_API_KEY`.
4. Run: `python3 main.py`
